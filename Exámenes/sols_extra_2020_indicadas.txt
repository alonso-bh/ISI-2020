- NoSQL: El mecanismo de parametrización de consultas que ofrezca el sistema NoSQL que le haya correspondido a cada uno.

- Teorema CAP: correo postal (AP), comercio electrónico (CA), HTML (CA), HTML5 (AP).

- Diseño de brokers: Uso de colas de mensajes y un pool de hebras. Más técnicamente, el patrón de diseño "half-sync/half-async"

- Inyección SQL:  Hay que construir una sentencia SQL usando UNION para añadir, a los datos de los pedidos de un usuario, los datos que aparecen en una tabla diferente de la base de datos, de forma que todo se acabe mostrando en la interfaz de usuario, p.ej. 

login4.jsp?user=a%27+UNION+select+id,password,email,0+from+%27usuarios&password=asdf%27or%271%27%3D%271

-- soluciones extraídas exactamente de la plataforma, publicadas por el profesor.